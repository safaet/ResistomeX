# --- real research data (never commit) ---
data/raw/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# --- Streamlit ---
.streamlit/secrets.toml

# --- OS / editor ---
.DS_Store
.idea/
.vscode/

# NOTE: models/**/*.joblib ARE committed so the hosted app has models without a
# build step. They are small (~140 KB each) and deterministic. If a newer
# scikit-learn is installed than the one they were pickled with, the app
# transparently retrains from the bundled CSVs, so a stale binary is harmless.

# --- large unrelated data dump (not used by the app) ---
Data/2024/

# --- editor/tool ---
.qodo/
